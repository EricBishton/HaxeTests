package tivo;

typedef DeviceRegistration = haxe.macro.MacroType<[TypeBuilder.build("DeviceRegistration.schema")]>;
//  Error is here --------------------------------^  at the open bracket.

class MacroTypeDefinition {
    public function new() {
    }
}
